export const getConfigButtons = () => {
    const connectButton = document.querySelector("#connect-btn");
    const updateButton = document.querySelector("#update-btn");

    return {
        connectButton,
        updateButton
    }
}